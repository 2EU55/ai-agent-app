# 🚀 AI 实习生特训营项目 (AI Intern Bootcamp)

欢迎来到 AI 实习生特训营项目！本仓库包含了一系列 AI 应用，从基础的聊天机器人到复杂的 RAG 系统和多智能体（Multi-Agent）工作流。

## 📂 项目结构

### 1. 🔰 基础入门 (Week 1)
- **`quick_start_ai.py`**: 一个简单的“Hello World” AI 应用，使用 Streamlit 和 OpenAI 构建。
- **`memory_chatbot.py`**: 一个能记住你名字和对话上下文的聊天机器人（使用摘要记忆）。
- **`app.py`**: 一个简单的待办事项（Todo List）应用，用于练习 Streamlit。

### 2. 🧠 RAG 系统 (Week 2)
- **`rag_app.py`**: 核心 RAG（检索增强生成）应用。它检索 `company_policy.txt` 中的信息来回答用户问题。
- **`refusal_rules.py`**: 包含拒绝回答不可回答问题的逻辑（控制幻觉）。
- **`rag_structure_aware.py`**: 高级文档切分工具，能够尊重 Markdown 结构。
- **`rag_eval.py`**: 用于测量 RAG 性能（精确率/召回率）的评测脚本。

### 3. 🤖 智能体与 LangGraph (Week 3-4)
- **`multi_agent_graph.py`**: **[核心]** 基于 LangGraph 构建的多智能体系统。它会将查询路由给“数据分析师”或“政策专家”。
- **`server.py`**: 数据分析 Agent 的后端 API（FastAPI）。提供 `/chat` 与 `/static/output.png`。
- **`data_agent_app.py`**: 数据分析 Agent 的 Streamlit Web 界面（调用 `server.py`）。
- **`agent_demo.py`**: 一个较简单的 Agent 演示，使用 LangChain 的 `create_agent` 和基础工具。
- **`data_analysis_agent.py`**: 专门用于分析 pandas DataFrame 并生成图表的 Agent 逻辑。
- **`chain_writer.py`**: 基于链（Chain）的文章生成器（大纲 -> 正文）。

### 4. 🛠️ 工具与配置
- **`pylib/`**: 本地库和依赖项。
- **`.env.example`**: 环境变量模板（请复制为 `.env`，不要提交）。
- **`company_policy.txt`**: 知识库源文件。
- **`sales_data.csv`**: 数据分析 Agent 使用的演示数据。

## 🚀 如何运行

## ✅ 5 分钟复现（面试官版本）

```bash
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

另开一个终端：

```bash
streamlit run data_agent_app.py
```

验收点：
- 输入“画一个销售额趋势图”→ 出现待确认代码 → 点“确认执行”→ 成功出图
- 输入政策问题（如“出差住宿报销标准是什么？”）→ 直接返回政策答案
- 运行回归评测：`python -u run_eval.py --auto-server --timeout 240` → `Failed: 0`

### 1. 运行 RAG 聊天机器人
```bash
streamlit run rag_app.py
```

### 2. 运行数据分析 Agent（后端 + 前端）
先启动后端：
```bash
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

再启动前端：
```bash
streamlit run data_agent_app.py
```

### 2.1 使用 Docker 一键启动（后端 + 前端）

在 `AI_Intern_Bootcamp/` 目录下：

```bash
docker compose up --build
```

可选：如需启用在线模型能力，请在运行前设置环境变量或 `.env`：
- `SILICONFLOW_API_KEY=...`

访问：
- 前端：http://localhost:8501
- 后端：http://localhost:8000

说明：
- `web` 容器默认通过环境变量 `API_URL=http://api:8000` 访问后端
- `api` 容器的运行产物默认写入持久化卷（`output.png` / `api_events.jsonl` / `events_summary.json`）
- `web` 会等待 `api` 健康检查通过后再启动，提高启动稳定性
- 若你在中国网络环境，建议在 Docker Desktop 配置可用的镜像加速/代理以确保能拉取基础镜像
- 如发现图表中文显示为方块/乱码，请重新 `docker compose up --build`，确保镜像内已安装中文字体（Noto CJK / 文泉驿）

说明：
- 数据分析（Analyst）默认启用“执行前确认”（Human-in-the-loop）：第一次请求会返回待执行代码与 `task_id`；前端支持一键确认/取消（或手动调用 `/confirm`）后才会真正执行并生成图表。
- 可以直接运行 [test_api.py](file:///c:/Users/czy/PycharmProjects/AI_Workspace/AI_Intern_Bootcamp/test_api.py) 来验证三条 Demo（general/analyst/expert）。

接口要点：
- `POST /chat`：正常对话入口，可能返回 `pending=true`、`task_id`、`code`
- `POST /confirm`：提交 `{"task_id": "...", "action": "confirm|cancel"}`
- `GET /tasks/{task_id}`：查看待确认任务（用于前端展示 code）
- 所有接口会返回结构化 `meta`（路由/耗时/确认动作等），并追加写入 `api_events.jsonl`
- `pending=true` 时会返回 `risk_report`（危险命中项列表），用于前端展示与审计
- 若 `risk_report.dangerous=true`，后端会拦截确认执行（`/confirm` 返回 403），前端按钮会置灰

错误返回（非 2xx）：
- FastAPI 标准字段 `detail` 为结构化对象：`{code, message, reason?, ...}`

事件汇总：
```bash
python -u analyze_events.py --input api_events.jsonl --output events_summary.json
```

### 3. 运行智能体演示（控制台）
```bash
python agent_demo.py
```

### 4. 运行文章生成器
```bash
streamlit run chain_writer.py
```

## ✅ CI 回归评测

仓库已配置 GitHub Actions：`.github/workflows/eval.yml`
- Push / PR 自动运行 `AI_Intern_Bootcamp/run_eval.py --auto-server`
- 自动上传 `eval_summary.json` / `eval_report.json` / `eval_failures.json` 作为构建附件

## 📚 文档
- **[项目地图 (Project Map)](project_map.md)**: 代码库的详细指南。
- **[架构图与数据流](docs/ARCHITECTURE.md)**: 请求流/状态流/数据流总览。
- **[面试深挖 Q&A](docs/INTERVIEW_QA.md)**: 对照代码的问答清单。
- **[30天学习计划](30_days_plan.md)**: 学习路线图和进度追踪。
- **[学习日志](learning_log.md)**: 记录坏案例（Bad Cases）和调试经验。
- **[LeetCode 刷题规划（AI 应用岗 A 路线）](leetcode_plan_ai_app_a.md)**: 28题高频题单与收藏方式。
- **[回归评测（Eval-Driven）](EVAL.md)**: 一键验证关键体验不回退。

## 🔧 设置
1.  确保你已安装 Python。
2.  安装依赖（检查 `requirements.txt` 或直接根据 import 安装）。
3.  复制 `.env.example` 为 `.env`，并填入你的 API Key：
    ```env
    SILICONFLOW_API_KEY=sk-......
    ```
