# 📝 知识点自测清单 (Self-Assessment)

这就是你觉得“很多代码没读懂”的原因——因为里面藏着很多新概念。
请对照下表，给自己打个勾。**找出“❌”最多的模块，就是我们需要重点攻克的地方。**

## 💼 你的 30 天“简历关键词” (Resume Keywords)
*做完这个训练，这些专业术语你可以自信地写进简历里：*

| 专业领域 | 简历关键词 (Keywords) | 你在这个项目里做了什么？ |
| :--- | :--- | :--- |
| **架构设计** | **RAG (检索增强生成)** | 搭建了一套完整的“检索-生成”流水线，解决了大模型幻觉问题。 |
| **数据处理** | **Vector Database (向量库)** | 使用 **ChromaDB** 存储和检索高维向量数据，实现毫秒级语义搜索。 |
| **核心算法** | **Embedding (语义向量化)** | 使用 **BGE-M3** 模型将企业文档转化为向量，理解文本深层语义。 |
| **工程落地** | **Prompt Engineering** | 设计 **Role-Playing** 和 **Few-Shot** 提示词，并通过 Context 约束输出格式。 |
| **质量控制** | **Evaluation (自动化评测)** | 搭建 **Eval-Driven** 开发流，使用 Precision/Recall 指标量化优化效果。 |
| **智能体** | **Agent & Tool Use** | 实现 **Function Calling**，让 AI 能够自主调用外部工具（搜索、计算）。 |
| **全栈开发** | **Streamlit** | 独立开发 AI 应用前端，处理 Session State 多轮对话状态管理。 |

---

## 1. Streamlit (前端界面)
| 知识点 | 哪里用到了? | 懂了吗? (✅/❌) | 简单解释 |
| :--- | :--- | :--- | :--- |
| `st.session_state` | `memory_chatbot.py` | | **网页的“短期记忆”**。刷新网页后，变量还能保存在这里。 |
| `st.chat_message` | `rag_app.py` | | 在界面上画出“气泡对话框”的组件。 |
| `st.sidebar` | `quick_start_ai.py` | | 左侧的侧边栏，通常放配置项（如 API Key 输入框）。 |

## 2. RAG 原理 (核心逻辑)
| 知识点 | 哪里用到了? | 懂了吗? (✅/❌) | 简单解释 |
| :--- | :--- | :--- | :--- |
| **Embedding (向量化)** | `rag_app.py` | | 把文字变成一串数字（如 `[0.1, 0.9, ...]`），方便算相似度。 |
| **Chunking (切分)** | `rag_structure_aware.py` | | 把长文章切成小块。切太碎会断章取义，切太大搜不准。 |
| **Vector DB (Chroma)** | `rag_app.py` | | 存向量的专用数据库。我们用它来做“语义搜索”。 |
| **Top-K** | `rag_app.py` | | 搜索时只取“最相似的前 K 个”片段给 AI 参考。 |
| **Temperature** | `rag_app.py` | | 控制 AI 的“创造力”。0 最死板（适合 RAG），1 最奔放。 |

## 3. LangChain (开发框架)
| 知识点 | 哪里用到了? | 懂了吗? (✅/❌) | 简单解释 |
| :--- | :--- | :--- | :--- |
| `PromptTemplate` | `chain_writer.py` | | **填空题模板**。把用户问题填进 `{question}` 坑位里。 |
| `Runnable / Chain` | `rag_app.py` | | **流水线**。用 `|` 符号把“检索 -> 组装 -> 回答”串成一条线。 |
| `Document` 对象 | `rag_structure_aware.py` | | LangChain 里对文本的包装，包含 `page_content` (正文) 和 `metadata` (来源)。 |

## 4. 进阶策略 (我们自己写的)
| 知识点 | 哪里用到了? | 懂了吗? (✅/❌) | 简单解释 |
| :--- | :--- | :--- | :--- |
| **Refusal Rule (拒答)** | `refusal_rules.py` | | **安全网**。如果搜出来的东西缺关键字段，强制让 AI 闭嘴。 |
| **Eval (评测)** | `rag_eval.py` | | **考试**。用标准答案去对 AI 的输出，算分。 |

## 5. Agent (智能体)
| 知识点 | 哪里用到了? | 懂了吗? (✅/❌) | 简单解释 |
| :--- | :--- | :--- | :--- |
| **Tool Calling (工具调用)** | `agent_demo.py` | | **AI 的手**。让大模型不仅能说话，还能调用 Python 函数（查库、计算）。 |
| **Code Interpreter (代码解释器)** | `data_analysis_agent.py` | | **AI 的大脑**。让大模型自己写 Python 代码并执行，解决复杂逻辑问题。 |
| **ReAct (推理+行动)** | `agent_demo.py` | | **思考过程**。先想“我要查什么”，再行动“查库”，最后回答。 |

---

## 🧐 接下来怎么做？
1. 把这个文件在 IDE 里打开。
2. 每一个你觉得“❌”的概念，都可以直接问我：“请用一个小学生能懂的例子解释一下什么是 Embedding？”
3. 或者告诉我：“我想把 RAG 原理这一块彻底搞懂”，我们就专门深挖那几个文件。
